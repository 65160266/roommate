# 🏠 ระบบหาเพื่อนร่วมห้อง RoomMate

## 📖 เกี่ยวกับโปรเจค

ระบบหาเพื่อนร่วมห้องสำหรับนิสิตมหาวิทยาลัย ช่วยให้นิสิตสามารถค้นหา จับคู่ และสื่อสารกับเพื่อนร่วมห้องที่มีความสนใจและบุคลิกภาพที่เข้ากันได้

---

## ✨ คุณสมบัติหลัก

### 👤 สำหรับผู้ใช้ทั่วไป
- ลงทะเบียนและเข้าสู่ระบบ
- กรอกข้อมูลส่วนตัว (คณะ, สาขา, บุคลิกภาพ)
- ค้นหาและกรองผู้ใช้
- ส่งคำขอจับคู่
- แชทส่วนตัวแบบ real-time
- สร้างและเข้าร่วมกลุ่มแชท
- สร้างประกาศหาเพื่อนร่วมห้อง

### 👨‍💼 สำหรับแอดมิน
- จัดการผู้ใช้ทั้งหมด
- จัดการกลุ่มแชททั้งหมด
- เข้าแชทกลุ่มได้ทุกกลุ่ม
- เตะสมาชิกออกจากกลุ่ม
- กำหนด Role ผู้ใช้

---

## 🛠️ เทคโนโลยีที่ใช้

- **Node.js** - JavaScript runtime
- **Express.js** - Web framework
- **MySQL** - ฐานข้อมูล
- **Socket.IO** - Real-time chat
- **EJS** - Template engine
- **Cloudinary** - เก็บรูปภาพ
- **bcryptjs** - เข้ารหัสรหัสผ่าน

---

## 📦 NPM Packages ที่ใช้

### **Dependencies (Production)**

1. **express** (v5.1.0)
   - Web framework หลักสำหรับสร้าง server
   - จัดการ HTTP requests/responses
   - กำหนด routes (URL endpoints)

2. **ejs** (v3.1.10)
   - Template engine สำหรับ render HTML
   - ใส่ JavaScript ลงใน HTML ได้
   - ส่งข้อมูลจาก server ไปแสดงที่หน้าเว็บ

3. **mysql2** (v3.14.1)
   - เชื่อมต่อกับฐานข้อมูล MySQL
   - รองรับ Promise API (async/await)
   - Connection pooling สำหรับประสิทธิภาพ

4. **bcryptjs** (v3.0.2)
   - เข้ารหัสรหัสผ่าน (password hashing)
   - เปรียบเทียบรหัสผ่านอย่างปลอดภัย
   - ป้องกันการถอดรหัสรหัสผ่าน

5. **express-session** (v1.18.1)
   - จัดการ session ของผู้ใช้
   - เก็บข้อมูลผู้ใช้ที่ล็อกอิน
   - ใช้ cookies เก็บ session ID

6. **connect-flash** (v0.1.1)
   - แสดงข้อความแจ้งเตือนชั่วคราว
   - ข้อความจะหายไปหลังแสดงครั้งเดียว
   - ใช้กับ success/error messages

7. **socket.io** (v4.8.1)
   - Real-time communication (WebSocket)
   - ส่งข้อความแชทแบบ real-time
   - แสดงสถานะ "กำลังพิมพ์..."

8. **multer** (v2.0.1)
   - จัดการการอัปโหลดไฟล์
   - รองรับ multipart/form-data
   - เก็บไฟล์ใน memory หรือ disk

9. **cloudinary** (v2.7.0)
   - เก็บรูปภาพบน cloud
   - อัปโหลดรูปผ่าน API
   - ได้ URL รูปภาพกลับมา (ฟรี 25GB)

10. **dotenv** (v17.2.1)
    - โหลด environment variables จากไฟล์ `.env`
    - เก็บข้อมูลสำคัญ (passwords, API keys)
    - ไม่ต้อง hard-code ในโค้ด

11. **body-parser** (v2.2.0)
    - Parse request body (JSON, URL-encoded)
    - Express 4.16+ มี built-in แล้ว

### **DevDependencies (Development)**

1. **nodemon** (v3.1.10)
   - Auto-restart server เมื่อมีการแก้ไขโค้ด
   - ไม่ต้อง restart server ด้วยตัวเอง
   - ใช้ในขณะพัฒนาเท่านั้น

---

## 🚀 วิธีติดตั้ง

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่า Environment Variables
สร้างไฟล์ `.env`:
```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=roommate

CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

SESSION_SECRET=secret_roommate
```

### 3. สร้างฐานข้อมูล
```sql
CREATE DATABASE roommate CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. รัน Server
```bash
npm start
```

### 5. เข้าใช้งาน
เปิดเบราว์เซอร์ที่: `http://localhost:3000`

---

## 📁 โครงสร้างโปรเจค

```
roommate/
├── config/              # ตั้งค่าระบบ
│   ├── cloudinary.js    # ตั้งค่า Cloudinary
│   └── database.js      # ตั้งค่า MySQL
│
├── controllers/         # ควบคุม Business Logic
│   ├── AdminController.js
│   ├── AccountsController.js
│   ├── AuthController.js
│   ├── ChatController.js
│   ├── GroupChatController.js
│   ├── HomeController.js
│   ├── MatchController.js
│   └── PostController.js
│
├── models/              # จัดการฐานข้อมูล
│   ├── AccountModel.js
│   ├── ChatModel.js
│   ├── GroupChatModel.js
│   ├── HomeModel.js
│   ├── MatchModel.js
│   ├── PassedModel.js
│   └── PostModel.js
│
├── routes/              # กำหนด URL
│   ├── adminRoutes.js
│   ├── accountRoutes.js
│   ├── authRoutes.js
│   ├── chatRoutes.js
│   ├── groupChatRoutes.js
│   ├── homeRoutes.js
│   ├── matchRoutes.js
│   └── postRoutes.js
│
├── middleware/          # Middleware
│   ├── authMiddleware.js
│   ├── flashMiddleware.js
│   └── upload.js
│
├── views/               # หน้าเว็บ (EJS)
│   ├── admin/           # หน้าแอดมิน
│   ├── partials/        # ส่วนที่ใช้ซ้ำ
│   └── *.ejs            # หน้าต่างๆ
│
├── public/              # ไฟล์ Static
│   ├── css/
│   ├── js/
│   └── images/
│
└── index.js             # ไฟล์หลัก
```

---

## 🗄️ ตารางในฐานข้อมูล

### ตารางหลัก
- **Register** - ข้อมูลการลงทะเบียน (email, password)
- **Accounts** - ข้อมูลผู้ใช้ (ชื่อ, อายุ, คณะ, สาขา)
- **Faculty** - ข้อมูลคณะ
- **Majors** - ข้อมูลสาขา
- **Personality** - บุคลิกภาพ
- **Accounts_has_Personality** - บุคลิกภาพของผู้ใช้

### ตารางประกาศและจับคู่
- **Post** - ประกาศหาเพื่อนร่วมห้อง
- **Matches** - การจับคู่

### ตารางแชท
- **Chat_Rooms** - ห้องแชทส่วนตัว
- **Messages** - ข้อความแชทส่วนตัว
- **Group_Chats** - กลุ่มแชท
- **Group_Members** - สมาชิกกลุ่ม
- **Group_Messages** - ข้อความในกลุ่ม

### ตารางระบบ
- **Roles** - บทบาทผู้ใช้ (Admin, User)

---

## 🔑 ฟังก์ชันหลักของแต่ละไฟล์

### Controllers (ควบคุมการทำงาน)

#### **AuthController.js**
- `showLogin()` - แสดงหน้า login
- `login()` - เข้าสู่ระบบ
- `showRegister()` - แสดงหน้าลงทะเบียน
- `register()` - ลงทะเบียน
- `logout()` - ออกจากระบบ

#### **AccountsController.js**
- `getAccount()` - แสดงหน้ากรอกข้อมูล
- `createAccount()` - สร้างข้อมูลผู้ใช้
- `getProfile()` - แสดงโปรไฟล์
- `showEditProfile()` - แสดงหน้าแก้ไขโปรไฟล์
- `updateProfile()` - อัปเดตโปรไฟล์
- `getMajorsByFaculty()` - ดึงรายการสาขาตามคณะ
- `getUserProfile()` - ดึงข้อมูลผู้ใช้สำหรับ modal

#### **HomeController.js**
- `getHome()` - แสดงหน้าแรก
- `searchUsers()` - ค้นหาผู้ใช้

#### **MatchController.js**
- `showMatched()` - แสดงรายการจับคู่สำเร็จ
- `showInProgress()` - แสดงรายการจับคู่ที่รอดำเนินการ
- `confirmMatch()` - ยืนยันการจับคู่
- `rejectMatch()` - ปฏิเสธการจับคู่
- `cancelMatch()` - ยกเลิกคำขอจับคู่

#### **ChatController.js**
- `showChatList()` - แสดงรายการแชท
- `startChat()` - เริ่มแชท
- `showChatRoom()` - แสดงห้องแชท
- `sendMessage()` - ส่งข้อความ

#### **GroupChatController.js**
- `showGroupChatList()` - แสดงรายการกลุ่ม
- `showCreateGroupForm()` - แสดงฟอร์มสร้างกลุ่ม
- `createGroup()` - สร้างกลุ่ม
- `showGroupChatRoom()` - แสดงห้องแชทกลุ่ม
- `sendGroupMessage()` - ส่งข้อความในกลุ่ม
- `leaveGroup()` - ออกจากกลุ่ม
- `removeMember()` - ลบสมาชิก

#### **PostController.js**
- `getMyPost()` - แสดงประกาศของฉัน
- `showCreatePost()` - แสดงฟอร์มสร้างประกาศ
- `createPost()` - สร้างประกาศ
- `showEditPost()` - แสดงฟอร์มแก้ไขประกาศ
- `updatePost()` - อัปเดตประกาศ
- `deletePost()` - ลบประกาศ

#### **AdminController.js**
- `dashboard()` - แสดง dashboard
- `getAllUsers()` - แสดงรายการผู้ใช้
- `getUserById()` - ดูรายละเอียดผู้ใช้
- `showEditUser()` - แสดงฟอร์มแก้ไขผู้ใช้
- `updateUser()` - อัปเดตผู้ใช้
- `deleteUser()` - ลบผู้ใช้
- `assignRoleToUser()` - กำหนด role
- `getAllGroups()` - แสดงรายการกลุ่ม
- `getGroupById()` - ดูรายละเอียดกลุ่ม
- `kickMember()` - เตะสมาชิก
- `deleteGroup()` - ลบกลุ่ม
- `updateGroupName()` - แก้ไขชื่อกลุ่ม

---

## 🔐 ความปลอดภัย

- รหัสผ่านเข้ารหัสด้วย **bcrypt**
- ใช้ **session** เก็บข้อมูลผู้ใช้
- ใช้ **prepared statements** ป้องกัน SQL injection
- ตรวจสอบสิทธิ์ด้วย **middleware**

---

## 🐛 การแก้ปัญหา

### ปัญหา: เชื่อมต่อ MySQL ไม่ได้
- ตรวจสอบว่า MySQL service ทำงานอยู่
- ตรวจสอบ username/password ในไฟล์ `.env`

### ปัญหา: Port 3000 ถูกใช้งานแล้ว
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### ปัญหา: อัปโหลดรูปไม่ได้
- ตรวจสอบ Cloudinary credentials
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต

---

## 📝 หมายเหตุ

- ไฟล์โค้ดทั้งหมดมี **comments ภาษาไทย** อธิบายการทำงาน
- อ่าน comments ในแต่ละไฟล์เพื่อเข้าใจการทำงาน
- ทุกฟังก์ชันมีคำอธิบายว่าทำอะไร

---

**พัฒนาโดย:** RoomMate Team  
**ปี:** 2025  
**เวอร์ชัน:** 1.0.0




 Controllers (8 ไฟล์)
✅ AuthController.js - จัดการการเข้าสู่ระบบและลงทะเบียน
✅ AccountsController.js - จัดการข้อมูลผู้ใช้และโปรไฟล์
✅ HomeController.js - จัดการหน้าแรกและการค้นหาผู้ใช้
✅ MatchController.js - จัดการการจับคู่เพื่อนร่วมห้อง
✅ ChatController.js - จัดการแชทส่วนตัว (1:1)
✅ GroupChatController.js - จัดการแชทกลุ่ม
✅ PostController.js - จัดการประกาศหาเพื่อนร่วมห้อง
✅ AdminController.js - จัดการระบบแอดมิน
📂 Models (6 ไฟล์)
✅ AccountModel.js - จัดการข้อมูลผู้ใช้ในฐานข้อมูล
✅ ChatModel.js - จัดการแชทส่วนตัวในฐานข้อมูล
✅ MatchModel.js - จัดการการจับคู่ในฐานข้อมูล
✅ PostModel.js - จัดการประกาศในฐานข้อมูล
✅ GroupChatModel.js - จัดการแชทกลุ่มในฐานข้อมูล
✅ HomeModel.js - จัดการข้อมูลหน้าแรกในฐานข้อมูล
📂 Routes (8 ไฟล์)
✅ authRoutes.js - เส้นทางสำหรับการเข้าสู่ระบบและลงทะเบียน
✅ accountRoutes.js - เส้นทางสำหรับจัดการข้อมูลผู้ใช้และโปรไฟล์
✅ homeRoutes.js - เส้นทางสำหรับหน้าแรกและการค้นหา
✅ matchRoutes.js - เส้นทางสำหรับการจับคู่
✅ chatRoutes.js - เส้นทางสำหรับแชทส่วนตัว
✅ groupChatRoutes.js - เส้นทางสำหรับแชทกลุ่ม
✅ postRoutes.js - เส้นทางสำหรับจัดการประกาศ
✅ adminRoutes.js - เส้นทางสำหรับระบบแอดมิน
📂 Middleware (3 ไฟล์)
✅ authMiddleware.js - ตรวจสอบสิทธิ์การเข้าถึง
✅ flashMiddleware.js - จัดการ flash messages
✅ upload.js - อัปโหลดไฟล์
📂 Config (2 ไฟล์)
✅ database.js - เชื่อมต่อ MySQL
✅ cloudinary.js - เชื่อมต่อ Cloudinary
📂 Views (17 ไฟล์)
หน้าหลัก (6 ไฟล์)
✅ index.ejs - หน้าแรก (Landing Page)
✅ login.ejs - หน้าเข้าสู่ระบบ
✅ register.ejs - หน้าลงทะเบียน
✅ home.ejs - หน้าแรก (แสดงผู้ใช้ทั้งหมด)
✅ accounts.ejs - หน้ากรอกข้อมูลผู้ใช้ครั้งแรก
✅ profile.ejs - หน้าโปรไฟล์ผู้ใช้
การจับคู่ (2 ไฟล์)
✅ matched.ejs - หน้าจับคู่สำเร็จ
✅ in-progress.ejs - หน้าจับคู่ที่รอดำเนินการ
แชท (2 ไฟล์)
✅ chat-room.ejs - หน้าแชทส่วนตัว (1:1)
✅ group-chat-room.ejs - หน้าแชทกลุ่ม
ประกาศและโปรไฟล์ (2 ไฟล์)
✅ create-post.ejs - หน้าสร้างประกาศหาเพื่อนร่วมห้อง
✅ edit-profile.ejs - หน้าแก้ไขโปรไฟล์
Admin Panel (5 ไฟล์)
✅ admin/dashboard.ejs - Dashboard แอดมิน
✅ admin/users.ejs - จัดการผู้ใช้
✅ admin/user-detail.ejs - รายละเอียดผู้ใช้
✅ admin/groups.ejs - จัดการกลุ่มแชท
✅ admin/group-detail.ejs - รายละเอียดกลุ่ม